name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    # --- UNIT TESTS ---
    - name: Run Tests
      run: npm test

    # --- SECURITY: SAST ---
    - name: Run SAST (Dependency Check)
      run: npm audit --audit-level=high || true # Show high severity issues but don't break build for demo

    # --- SECURITY: DAST ---
    - name: Build Local Image for DAST
      run: docker build -t local-test-image .

    - name: Run Container for DAST
      run: |
        docker run -d -p 3000:3000 --name test-container local-test-image
        sleep 5 # Wait for server to start

    - name: Run DAST (OWASP ZAP)
      uses: zaproxy/action-baseline@v0.14.0
      with:
        target: 'http://localhost:3000/health'
        fail_action: false
        allow_issue_writing: false
        artifact_name: 'zap-scan' # Fix "invalid name" error

    - name: Stop Test Container
      run: docker rm -f test-container

    # --- DEPLOY (PUSH) ---
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Login to Docker Hub
      if: github.event_name != 'pull_request'
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and Push
      uses: docker/build-push-action@v4
      with:
        context: .
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ secrets.DOCKER_USERNAME }}/student-registry:latest
